export enum DisplayType {
  AUTO, // automatically set's a view
  LINEAR, // shows a list like view
  TABLE, // shows a table like view
  GRID, // shows a grid view
  INFO, // shows a simple info about an object
}

export enum DataType {
    TEXT,
    IMAGE,
    LINK,
}